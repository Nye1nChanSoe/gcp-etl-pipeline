[project]
name = "extractor"
version = "0.1.0"
description = "Fetches open exchange rate data and saves it to GCS as a JSON file. Triggered by: HTTP (Cloud Scheduler)"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "functions-framework>=3.10.1",
    "requests>=2.33.1",
]
