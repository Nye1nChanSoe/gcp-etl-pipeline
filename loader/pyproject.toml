[project]
name = "loader"
version = "0.1.0"
description = "Reads a JSON file from GCS and loads its records into BigQuery. Triggered by: GCS object finalize (Eventarc)"
readme = "README.md"
requires-python = ">=3.11"
dependencies = []
